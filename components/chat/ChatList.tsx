import UserMenu from "./UserMenu";

const ChatList = () => {
  return (
    <main className="relative md:w-[40vw] xl:w-[35vw] 2xl:w-[30vw] h-screen bg-zinc-900">
      <UserMenu />
    </main>
  );
};

export default ChatList;
